# Optional. Only needed if you want to bill via the Anthropic API instead of
# your Claude Code login (which is used automatically otherwise).
#
# Copy this file to `.env` and fill in a real key - `.env` is gitignored so
# it never gets committed.
ANTHROPIC_API_KEY=
